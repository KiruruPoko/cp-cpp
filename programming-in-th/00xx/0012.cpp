#include <bits/stdc++.h>
using namespace std; 
typedef long long ll;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string input; 
    cin >> input; 
    string fl, sl, tl, fol, fil; 
    for (int i = 1; i < input.length(); i++){
        if (i != input.length()){
            if (i % 3 == 0)
                fl += "..*.";
                sl += ".*.*";
                tl += ""
        }
    }
}
