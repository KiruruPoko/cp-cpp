#include <bits/stdc++.h>
using namespace std; 
typedef long long ll;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int k, n;
    cin >> k >> n;
    int rank[10][20];
    int better[20][20];
    for (int i = 0; i < k; i++){
        for (int j = 0; j < n; i++){
            cin >> rank[k][n];
            rank[k][n]--;
        }
    }   

}
