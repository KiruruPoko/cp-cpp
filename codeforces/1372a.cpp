#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
    cin.tie(nullptr) -> sync_with_stdio(false);
    int t;
    cin >> t; 
    while (t--){
        int n; 
        cin >> n;
        vector<int> v(n, 1);
        for (auto &a: v) cout << a << " ";
        cout << '\n';
    }
    return 0;
}
