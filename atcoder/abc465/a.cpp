#include <bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(nullptr) -> sync_with_stdio(false);
    double a;
    double b; 
    cin >> a >> b; 
    cout << (a > ((b * 2) / 3)? "Yes": "No") << '\n';
}